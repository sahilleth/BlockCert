# BlockCert on Railway — service configuration reference
#
# Create THREE services in one Railway project:
#   1. MySQL      (Database plugin)
#   2. blockcert-api   → Dockerfile.backend + railway.backend.toml
#   3. blockcert-web   → Dockerfile.frontend + railway.frontend.toml
#
# Env templates: railway/backend.env.example, railway/frontend.env.example
# Full guide: docs/RAILWAY_DEPLOYMENT.md
