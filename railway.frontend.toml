# Copy/reference when creating the Frontend service on Railway.
# In the Railway dashboard: Settings → Config-as-code → path: railway.frontend.toml

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.frontend"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
